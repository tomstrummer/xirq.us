// sample keys that will get pulled into the app:
// rename it to keys.js and *don't check it in!*

module.exports = {
	maps_api_key : "your api key here",
	embedly_key : "embedly key",
	fb_app_id : "fb app id",
	session_secret : "session secret key"
}
